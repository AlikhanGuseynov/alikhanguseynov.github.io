$(window).ready(function(b){
     var elem=$('#i_1');                         //movement element
    var factor = 0.02;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});

$(window).ready(function(b){
     var elem=$('#i_2');                         //movement element
    var factor = 0.08;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});

$(window).ready(function(b){
     var elem=$('#i_3');                         //movement element
    var factor = 0.04;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});

$(window).ready(function(b){
     var elem=$('#i_4');                         //movement element
    var factor = -0.07;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});

$(window).ready(function(b){
     var elem=$('#i_5');                         //movement element
    var factor = -0.05;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});

$(window).ready(function(b){
     var elem=$('#i_6');                         //movement element
    var factor = -0.02;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});

$(window).ready(function(b){
     var elem=$('#i_7');                         //movement element
    var factor = -0.1;                          //movement coefficient
    var elem_left = elem.offset().left;
    var elem_top = elem.offset().top;
    console.log(elem_left, elem_top );
    $('.home').mousemove(function(e) {           //the area in which the mouse is moving 
      var X = e.pageX;                          //X axis position
      var Y = e.pageY;                          //Y axis position
      if (X > 0) {
        elem.animate({
        left: elem_left-(X*factor) + "px",
        top: elem_top-(Y*factor) + "px",
        position: "absolute",
        }, 1);
      } else {
      };
    });
});





